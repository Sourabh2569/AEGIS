# ADR 0039: Read-Only Provider Activation Model

AEGIS starts provider activation in `Provider setup required` unless backend configuration, rights, and health verification exist. This prevents fixture-backed data from being displayed as actual provider data.
