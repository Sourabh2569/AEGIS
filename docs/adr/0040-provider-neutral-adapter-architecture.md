# ADR 0040: Provider-Neutral Adapter Architecture

AEGIS extends provider adapters through a neutral read-only contract before selecting a provider-specific SDK. Provider calls remain backend-only and route through governed ingestion services.
