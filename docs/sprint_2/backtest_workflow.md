# Backtest Workflow

Governed dataset → point-in-time features → frozen manifest → baseline strategy → risk assessment → next-session-open execution assumption → costs and slippage → T+1 settlement → NAV/report.

The current implementation is fixture-backed and in-memory.
