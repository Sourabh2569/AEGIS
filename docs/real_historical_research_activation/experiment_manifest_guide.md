# Experiment Manifest Guide

Actual-data experiments require a frozen manifest before execution.

The manifest must include classification, data origin, strategy version, dataset version, universe, benchmark, feature versions, cost schedule, slippage model, settlement model, risk profile, execution model, partition dates, rejection criteria, stress scenarios, software commit, creator, and freeze timestamp.

Classification is always `ACTUAL_HISTORICAL_RESEARCH_ONLY`.
