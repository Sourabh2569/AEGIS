# Data Lineage Investigation Runbook

Trace a research run through provider, raw object, dataset version, normalized/curated layers, validation results, instrument master, universe, corporate actions, feature run, manifest, backtest, and evidence package.

If any link is missing, classify the run as blocked or incomplete.
